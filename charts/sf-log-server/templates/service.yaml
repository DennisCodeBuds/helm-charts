apiVersion: v1
kind: Service
metadata:
  name: {{ include "sf-log-server.fullname" . }}
  labels:
    {{- include "sf-log-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sf-log-server.selectorLabels" . | nindent 4 }}
