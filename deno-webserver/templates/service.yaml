apiVersion: v1
kind: Service
metadata:
  name: {{ include "deno-webserver.fullname" . }}
  labels:
    {{- include "deno-webserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deno-webserver.selectorLabels" . | nindent 4 }}
